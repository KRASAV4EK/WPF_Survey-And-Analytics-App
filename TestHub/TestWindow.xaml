<Window x:Class="TestHub.TestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding Project.ProjTitle}" Height="450" Width="600" ResizeMode="NoResize"
        DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Questions and Answers -->
        <ScrollViewer Grid.Row="0" Margin="10">
            <ItemsControl ItemsSource="{Binding Project.ProjQuestions}" AlternationCount="1000">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,0,0,10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <!-- Question Number -->
                            <TextBlock Grid.Row="0" Grid.Column="0" 
                                       Text="Question"
                                       Margin="0,5,10,5" FontWeight="Bold" />

                            <!-- Question Text -->
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding QuestionText}" Margin="5" />

                            <!-- Answers -->
                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Answer:" Margin="0,5,10,5" FontWeight="Bold" />
                            <ComboBox Grid.Row="1" Grid.Column="1" 
                                      ItemsSource="{Binding Answers}" 
                                      DisplayMemberPath="AnswerText"
                                      SelectedItem="{Binding SelectedAnswer}"
                                      Margin="5"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- OK and Cancel Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Name="OKButton" Content="OK" Width="80" Margin="5" Click="OKButton_Click" IsEnabled="{Binding CanSubmit, UpdateSourceTrigger=PropertyChanged}" />
            <Button Name="CancelButton" Content="Cancel" Width="80" Margin="5" Click="CancelButton_Click" />
        </StackPanel>
    </Grid>
</Window>
